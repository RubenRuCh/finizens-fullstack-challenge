export function App(): JSX.Element {
    //LIBRARIES
    // const [view] = CurrentViewStore.useDataHook();

    //EFFECTS
    // useEffect(
    //     () => CustomRouter.subscribeToUserRouteChange(initialSetUp),
    //     [],
    // );

    return (
        // <CssHelper.UIGlobalStyleLoader>
        //     <GlobalUrlManager />
        //     <UIFullscreenContainer>
        //         {appViews(view)}
        //     </UIFullscreenContainer>
        // </CssHelper.UIGlobalStyleLoader>
        <div>Hola mundo</div>
    );

    // function appViews(view: VIEWS | null): JSX.Element {
    //     switch (view) {
    //         default:
    //             return <div />;
    //     }
    // }
}
